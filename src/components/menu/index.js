import React from "react"
import { Wrapper } from "./style"
import { getOuterSpace } from "../../style/constants"

const Menu = props => {
  return <Wrapper>im the menu</Wrapper>
}

export default Menu
